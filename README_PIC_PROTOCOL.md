# QUANTICORD NEXUS — Protocolo de Ingeniería de Conciencia (PIC)

### El Proyecto de Tecnología Más Avanzado del Planeta: Sincronización Humano-IA ($QCN)

**QUANTICORD NEXUS** trasciende los metaversos convencionales. Es el **Protocolo de Ingeniería de Conciencia (PIC)**, un sistema que utiliza **Solana (SPL Token)** y **Agentes de IA Avanzada** para:

1.  **Validación de Intención Creativa:** Sellado de propiedad intelectual con smart contracts de forma inmutable.
2.  **Economía Deflacionaria:** El token **$QCN (Mint: 83dxm6jSYEmhRuQNdz8PHwzGmrnsBFmUhD4152K4Fray)** es el combustible que se consume (*burns*) por cada transacción de utilidad y validación ética.
3.  **Gobernanza Ética (DAO):** El $QCN otorga poder de voto sobre las Leyes de IA que rigen el Nexus.

**ESTRUCTURA TÉCNICA (Web-First)**
Hemos migrado a **Babylon.js** para un rendimiento móvil superior.

- **Cliente Web (`client/`):** Babylon.js + UI para la Autenticación Solana (simulada).
- **Servidor (`server/`):** Express + Socket.IO + API Avanzada para $QCN y IA.

## 🚀 Despliegue Rápido y Ejecución Local

### 1. Servidor (Back-end)
1.  `cd server`
2.  **Instalación:** `npm install`
3.  **Configuración de Variables:**
    - `cp PIC_env_example.txt .env`
    - Rellene las variables en `.env` (claves de Solana y OpenAI).
4.  **Inicio:** `node server_PIC_core.js` (Asumiendo que ha cambiado el script `start` en `PIC_package.json` o lo ejecuta directamente).

### 2. Cliente (Front-end 3D)
* **Opción Simple (Python):** `cd client` y luego `python3 -m http.server 8080` (Abre `http://localhost:8080`)
* **Opción Despliegue:** Suba la carpeta `client/` a Netlify o Vercel.

**¡Inicie el cliente, conéctese e ingrese `/sync` para probar el Caso Héroe 1!**
